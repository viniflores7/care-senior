/// Define se `ResidentDetailScreen` é aberta em modo leitura (responsável)
/// ou em modo edição (funcionário da clínica).
class ViewerRole {
  ViewerRole._();

  static const String guardian = 'guardian';
  static const String staff = 'staff';
}
